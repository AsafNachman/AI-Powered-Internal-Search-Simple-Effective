# --- Web layer -------------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
httpx>=0.27,<1.0

# --- AI / RAG --------------------------------------------------------------
# LangChain 1.x line. langchain-ollama supplies both ChatOllama and
# OllamaEmbeddings, so no cloud provider SDK is ever imported.
langchain-core>=1.0,<2.0
langchain-ollama>=1.0,<2.0
langchain-text-splitters>=1.0,<2.0
langgraph>=1.0,<2.0

# --- Vector store ----------------------------------------------------------
# Used directly (not via langchain-chroma) so the persistence layer stays
# behind our own repository interface. See app/core/vectorstore.py.
chromadb>=1.0,<2.0

# --- Document content extraction ------------------------------------------
pypdf>=6.0,<7.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
python-pptx>=1.0,<2.0
charset-normalizer>=3.3,<4.0

# --- Filesystem watching -----------------------------------------------------
# Native OS notifications (ReadDirectoryChangesW / FSEvents / inotify) behind
# one cross-platform API, for live re-indexing. See app/core/watcher.py.
watchdog>=4.0,<7.0
