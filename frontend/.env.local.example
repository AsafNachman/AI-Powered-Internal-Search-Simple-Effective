# Copy to frontend/.env.local only if the backend is not on the default port.
# next.config.ts rewrites /api/* to this origin, so the browser never issues a
# cross-origin request and CORS never applies.
BACKEND_URL=http://127.0.0.1:8000
